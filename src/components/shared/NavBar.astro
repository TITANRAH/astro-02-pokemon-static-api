---
const links = [
  {
    href: "/",
    text: "Listado",
  },
  {
    href: "/pokemons/1",
    text: "Paginado",
  },
  {
    href: "/favorites",
    text: "Favoritos",
  },
  {
    href: "/islas",
    text: "Islas",
  },
];

// asi obtengo la url el path
const currentPath = Astro.url.pathname;

console.log(currentPath);
---

<!-- component -->
<nav class="shadow bg-slate-900">
  <div
    class="container flex items-center justify-center p-6 mx-auto text-gray-600 capitalize dark:text-gray-300"
  >
    <!-- {
      links.map((link) => (
        <a
          href={link.href}
          class:list={[
            "  hover:text-gray-800 dark:hover:text-gray-200 mx-1.5 sm:mx-6 hover:border-blue-500",
            {
              "border-b-2 border-blue-500 ": currentPath === link.href,
            },
          ]}
        >
          {link.text}
        </a>
      ))
    } -->
    {
      links.map((link) => (
        <div>
          <a
            href={link.href}
            class="hover:text-gray-800 dark:hover:text-gray-200 mx-1.5 sm:mx-6"
          >
            {link.text}
          </a>

          {currentPath === link.href ? (
            <div transition:name="menu-line" class="border-b-2 border-blue-500 mx-4" />
          ): (
            <div class="border-b-2 border-transparent"/>
          )}
        </div>
      ))
    }
  </div>
</nav>
